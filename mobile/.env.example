# KinSpace mobile app configuration
# Copy to .env.local and adjust.

# Optional: pin the API base URL explicitly.
# When unset, the app reuses the Expo dev-server host (port 8080).
EXPO_PUBLIC_API_URL=http://localhost:8080/api/v1
